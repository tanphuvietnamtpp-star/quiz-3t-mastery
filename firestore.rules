rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Standard rule matching all collections to allow smooth preview operations
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
